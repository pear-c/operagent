# Anthropic (진단 LLM) — 없으면 결정론 수집 사실만 게시 (graceful degrade)
ANTHROPIC_API_KEY=
OPERAGENT_MODEL=claude-opus-4-8

# Slack (chat.postMessage) — Bot을 대상 채널에 /invite 해둘 것
SLACK_BOT_TOKEN=
SLACK_CHANNEL_ID=

# 관측 스택 (컨테이너 네트워크 서비스명)
PROMETHEUS_URL=http://prometheus:9090
LOKI_URL=http://loki:3100
